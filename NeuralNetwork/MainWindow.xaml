<Window x:Class="NeuralNetwork.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NeuralNetwork"
        xmlns:viewmodel="clr-namespace:NeuralNetwork.ViewModel"
        xmlns:conv="clr-namespace:NeuralNetwork.Converters"
        d:DataContext="{d:DesignInstance viewmodel:ParametersViewModel, IsDesignTimeCreatable=True}"
        mc:Ignorable="d" Title="MLP Neural Network" Height="600" MinHeight="600" Width="900" MinWidth="900" FontSize="20" ResizeMode="NoResize">
    <Window.Resources>
        <conv:BooleanInverter x:Key="BooleanInverter" />
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="220" />
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                    <ColumnDefinition Width="150"/>
                </Grid.ColumnDefinitions>

                <Grid Grid.Column="0">
                    <Button x:Name="BT_TreinarRede"
                            Click="BT_TreinarRede_Click"
                            Width="200"
                            Cursor="Hand"
                            Content="Treinar Rede"
                            Height="120">
                        <Button.Resources>
                            <Style TargetType="{x:Type Border}">
                                <Setter Property="CornerRadius"
                                        Value="10" />
                            </Style>
                        </Button.Resources>
                    </Button>

                    <ProgressBar Minimum="0"
                                 Maximum="100"
                                 Margin="10,200,10,0"
                                 Value="{Binding TrainingProgress}"
                                 IsIndeterminate="False"
                                 Height="10"
                                 x:Name="progressBar">
                        <ProgressBar.Style>
                            <Style TargetType="ProgressBar">
                                <Setter Property="Visibility"
                                        Value="Collapsed" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsTraining}"
                                                 Value="True">
                                        <Setter Property="Visibility"
                                                Value="Visible" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </ProgressBar.Style>

                    </ProgressBar>
                </Grid>


                <Button x:Name="BT_TestarRede"
                        Click="BT_TestarRede_Click"
                        Width="200"
                        Cursor="Hand"
                        Content="Testar Rede"
                        Height="120"
                        Grid.Column="1"
                        IsEnabled="{Binding IsTrained}">
                    <Button.Resources>
                        <Style TargetType="{x:Type Border}">
                            <Setter Property="CornerRadius"
                                    Value="10" />
                        </Style>
                    </Button.Resources>
                </Button>

                <Grid Grid.Column="2">
                    <Grid.RowDefinitions>
                        <RowDefinition />
                        <RowDefinition Height="auto"/>
                    </Grid.RowDefinitions>

                    <GroupBox Grid.Row="1"
                                        Header="Resultados"
                                        FontSize="14">
                        <StackPanel Margin="0,10">
                            <TextBlock Text="{Binding TestReturnMLP.TotalCases, StringFormat='Qtde. testes: {0}'}"/>
                            <TextBlock Text="{Binding TestReturnMLP.QtyCorrect, StringFormat='Qtde. correta: {0}'}" />
                            <TextBlock Text="{Binding TestReturnMLP.QtyWrong, StringFormat='Qtde. errada: {0}'}" />
                            <TextBlock Text="{Binding TestReturnMLP.Accuracy, StringFormat='Acurácia: {0:P2}'}"
                                       Margin="0,5"/>
                            <TextBlock Text="Precisão por classe" >
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding TestReturnMLP}"
                                                         Value="{x:Null}">
                                                <Setter Property="Visibility"
                                                        Value="Hidden" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>                                
                                </TextBlock.Style>
                            </TextBlock>
                            <ItemsControl ItemsSource="{Binding TestReturnMLP.Precision}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="{Binding Key, StringFormat=' {0:P2}'}"/>
                                            <TextBlock Text=": "/>
                                            <TextBlock Text="{Binding Value, StringFormat=' {0:P2}'}"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                            <Button x:Name="BT_SalvarResultados"
                                    Click="BT_SalvarResultados_Click"
                                    Cursor="Hand"
                                    Content="Salvar Resultados"
                                    Margin="0,10,0,0"
                                    Grid.Column="0">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding TestReturnMLP}"
                                                         Value="{x:Null}">
                                                <Setter Property="Visibility"
                                                        Value="Hidden" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>                                
                                </Button.Style>
                                <Button.Resources>
                                    <Style TargetType="{x:Type Border}">
                                        <Setter Property="CornerRadius"
                                                Value="10" />
                                    </Style>
                                </Button.Resources>
                            </Button>
                        </StackPanel>

                    </GroupBox>

                </Grid>

            </Grid>
            
            <GroupBox Grid.Row="1"
                      Header="Arquivos"
                      Margin="5,5,0,5"
                      IsEnabled="{Binding IsTraining, Converter={StaticResource BooleanInverter}}">

                <StackPanel>

                    <GroupBox Header="Treino"
                              FontSize="12">

                        <StackPanel>
                            <StackPanel Orientation="Horizontal"
                                         Margin="0,5">
                                <TextBox IsReadOnly="True"
                                     Grid.Column="0"
                                     Width="350"
                                     Name="TBTrainFile"
                                         Margin="0,0,5,0"
                                         Height="28"
                                         Text="{Binding TrainFilePath}"></TextBox>
                                <Button Grid.Column="1"
                                        Padding="8,0"
                                        Width="210"
                                        FontSize="14"
                                        Cursor="Hand"
                                        Content="Selecionar arquivo de entrada"
                                        Name="BTTrainFile"
                                        Click="BTTrainFile_Click">
                                    <Button.Resources>
                                        <Style TargetType="{x:Type Border}">
                                            <Setter Property="CornerRadius"
                                                    Value="5" />
                                        </Style>
                                    </Button.Resources> 
                                </Button>
                            </StackPanel>

                            <StackPanel Orientation="Horizontal">

                            <TextBox IsReadOnly="True"
                                 Grid.Column="0"
                                Width="350"
                                Height="28"
                                Name="TBOutputTrainFile"
                                 Margin="0,0,5,0"
                                 Text="{Binding TrainDummiesFilePath}" />

                            <Button Grid.Column="1"
                                 Padding="8,0"
                                    Width="210"
                                    FontSize="14"
                                    Cursor="Hand"
                                    Name="BTOutputTrainFile"
                                    Content="Selecionar arquivo dummy"
                                 Click="BTOutputTrainFile_Click">
                                <Button.Resources>
                                    <Style TargetType="{x:Type Border}">
                                        <Setter Property="CornerRadius"
                                                Value="5" />
                                    </Style>
                                </Button.Resources> 
                            </Button>

                        </StackPanel>

                        </StackPanel>

                    </GroupBox>

                    <GroupBox Header="Teste" FontSize="12">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal"
                                        Margin="0,5,0,0">

                                <TextBox IsReadOnly="True"
                                         Grid.Column="0"
                                         Width="350"
                                         Height="28"
                                         Name="TBTestFile"
                                         Margin="0,0,5,0"
                                         Text="{Binding TestFilePath}" />

                                <Button Grid.Column="1"
                                        Padding="8,0"
                                        Cursor="Hand"
                                        Width="210"
                                        FontSize="14"
                                        Content="Selecionar arquivo de entrada"
                                        Name="BTTestFile"
                                        Click="BTTestFile_Click">
                                    <Button.Resources>
                                        <Style TargetType="{x:Type Border}">
                                            <Setter Property="CornerRadius"
                                                    Value="5" />
                                        </Style>
                                    </Button.Resources>
                                </Button>

                            </StackPanel>

                            <StackPanel Orientation="Horizontal"
                                        Margin="0,5">

                                <TextBox IsReadOnly="True"
                                         Grid.Column="0"
                                         Width="350"
                                         Name="TBTestOutputFile"
                                         Margin="0,0,5,0"
                                         Text="{Binding TestDummiesFilePath}" />

                                <Button Grid.Column="1"
                                        Padding="8,0"
                                        Cursor="Hand"
                                        Width="210"
                                        Height="28"
                                        FontSize="14"
                                        Content="Selecionar arquivo dummy"
                                        Name="BTTestOutputFile"
                                        Click="BTTestOutputFile_Click">
                                    <Button.Resources>
                                        <Style TargetType="{x:Type Border}">
                                            <Setter Property="CornerRadius"
                                                    Value="5" />
                                        </Style>
                                    </Button.Resources>
                                </Button>

                            </StackPanel>
                        </StackPanel>                    
                
                </GroupBox>
                
                </StackPanel>

            </GroupBox>  
        </Grid>
        
        <GroupBox Grid.Column="1" 
                  Header="Parâmetros"
                  Margin="5">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="200"/>
                    <RowDefinition />
                </Grid.RowDefinitions>
                <StackPanel Grid.Row="0">

                    <StackPanel Margin="0,10,0,0">
                        <Label FontSize="10" Height="25">Épocas</Label>
                        <TextBox Name="TBIterations" 
                                    Margin="10,0"
                                 Text="{Binding Iterations, Mode=TwoWay, StringFormat=\{0:N0\}, ConverterCulture=pt-br}"/>
                    </StackPanel>

                    <StackPanel Margin="0,10,0,0">
                        <Label FontSize="10" Height="25">Taxa de Aprendizado</Label>
                        <TextBox Name="TBLearningRate"
                                 Margin="10,0"
                                 Text="{Binding LearningRate, Mode=TwoWay, StringFormat=\{0:N2\}, ConverterCulture=pt-br}" />

                    </StackPanel>

                </StackPanel>

                <Grid Grid.Row="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto"/>
                        <RowDefinition />
                    </Grid.RowDefinitions>

                    <Label Grid.Row="0">Camadas Internas</Label>
                    <DataGrid ItemsSource="{Binding Layers}"
                              FontSize="14"
                              Grid.Row="1"
                              CanUserSortColumns="False"
                              AutoGenerateColumns="False"
                              CanUserAddRows="True"
                              CanUserDeleteRows="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Camada"
                                                Binding="{Binding Index}"
                                                Width="3*"
                                                IsReadOnly="True" />
                            <DataGridTextColumn Header="Qtde. Neuronios"
                                                Width="5*"
                                                Binding="{Binding QtyNeurons}" />
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>               
                
            </Grid>
            
        </GroupBox>
    </Grid>
</Window>
